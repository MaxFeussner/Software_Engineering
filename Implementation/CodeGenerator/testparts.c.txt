#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include<stdbool.h>
#include<ctype.h>
#include <ctap.h>

TESTS {
    //test for tokentype
    ok(tokentype('+') == 3,"Its a simbol");
    ok(tokentype('-') == 3,"Its a simbol");
    ok(tokentype('/') == 3,"Its a simbol");
    ok(tokentype('/') == 3,"Its a simbol");
    ok(tokentype('%') == 3,"Its a simbol");
    ok(tokentype(43) == 0,"Its an int");
    ok(tokentype(43.43) == 1,"Its an float");
    ok(tokentype("H") == -1,"Its an error");
    //test for codegenerator
    is(codeGenerator('+',"ADD","Its ADD);
    is(codeGenerator('-',"SUB","Its SUB);
    is(codeGenerator('/',"DIV","Its DIV);
    is(codeGenerator('*',"MUL","Its MUL);
    is(codeGenerator('%',"MOD","Its MOD);
    is(codeGenerator(43,"LDI 43","Its an int);
    is(codeGenerator(43.43,"LDF 43.43","Its a float);
    is(codeGenerator("HJ","Error there is no case","Its not recognize");
    
    
    
}